{% if posts %}
  <ul>
    {% for post in posts %}
      <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </li>
    {% endfor %}
  </ul>
{% else %}
  <p>Бюллетеней за этот год еще не появилось.</p>
{% endif %}
