<h2>Архив</h2>
{% set years = collections.subjexByYear | getYears %}
{% set globalCurrentYear = getGlobalCurrentYear | int %}
{## Извлекаем год из текущего URL для определения активной ссылки ##}
{%- set currentPath = page.url -%}
{%- set currentYear = currentPath | replace("/subjex/", "") | replace("/", "") -%}
{%- set isRootPage = currentPath == "/subjex/" or currentPath == "/subjex" -%}
{% for year in years %}
{## Применяем класс active-link к ссылке, если её год совпадает с текущим годом из URL ##}
{% if isRootPage and (year | int == globalCurrentYear) %}
  {# Текущий год на корневой странице /subjex/ уже выводится в index #}
{% else %}
  <a href="/subjex/{{ year }}/"{% if year == currentYear %} class="active-link"{% endif %}>{{ year }}</a>
{% endif %}
{% endfor %}
