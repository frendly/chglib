{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install All Recommended Extensions",
      "type": "shell",
      "windows": {
        "command": "powershell",
        "args": [
          "-ExecutionPolicy",
          "Bypass",
          "-File",
          "${workspaceFolder}/.vscode/install_extensions.ps1"
        ]
      },
      "osx": {
        "command": "bash",
        "args": [
          "-c",
          "EDITOR=$([ -n \"$CURSOR_PID\" ] || [ -n \"$CURSOR_SESSION_ID\" ] && echo 'cursor' || (command -v cursor >/dev/null 2>&1 && echo 'cursor' || echo 'code')); node .vscode/install_extensions.js | xargs -L 1 $EDITOR --install-extension"
        ]
      },
      "linux": {
        "command": "bash",
        "args": [
          "-c",
          "EDITOR=$([ -n \"$CURSOR_PID\" ] || [ -n \"$CURSOR_SESSION_ID\" ] && echo 'cursor' || (command -v cursor >/dev/null 2>&1 && echo 'cursor' || echo 'code')); node .vscode/install_extensions.js | xargs -L 1 $EDITOR --install-extension"
        ]
      },
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "silent"
      },
      "problemMatcher": []
    }
  ]
}
